export default function SceneClock({ gameId }: { gameId: string }) {
  return (
    <div className="bg-white p-3 rounded shadow">
      <div className="font-semibold">Scene & Clocks</div>
      <div className="text-sm text-slate-600">TODO: Heat, countdowns, etc.</div>
    </div>
  );
}
